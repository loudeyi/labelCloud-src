# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# Virtual environments
.venv/
venv/

# labelCloud runtime artefacts — only at the repository root, i.e. the directory
# labelCloud is launched from. Anchoring with a leading slash keeps patterns like
# `labels/` from also ignoring the source package `labelCloud/io/labels/`.
/config.ini
/pointclouds/
/labels/
/calib/
/segmentation/
/config.ini.bak
/.bak/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
